# Woodstove 2 — Промт для незалежної перевірки

Скопіюй текст нижче як завдання іншій моделі / тестеру.

---

**РОЛЬ:** ти — незалежний QA-інженер і рецензент коду. Перевір проєкт **Woodstove 2** на коректність, узгодженість і відсутність помилок. Не довіряй описам — перевіряй факти.

**ОБʼЄКТ:**
- Live: `https://olehcheff.github.io/woodstove2/` (відкривати після `Ctrl+F5`; за потреби кнопка «Скинути»).
- Локально: теки `js/`, `tests/`. Запуск тестів: `node tests/physics.test.js`.
- Дозволено: читати файли, запускати node, аналізувати дані. Заборонено: змінювати код.

**ЗАВДАННЯ (виконати всі):**

### 1. Статичний аудит коду
- Перевір, що кожен `getElementById('X')` в `app.js` має відповідний `id="X"` в `index.html`.
- Перевір, що кожен ключ `t('key')` і `data-i18n="key"` існує в **обох** словниках (`uk` і `en`) у `i18n.js`.
- Знайди мертвий код, дублікати, невикористані змінні/імпорти, потенційні `ReferenceError`.
- Перевір `normalizeConfig`: чи всі поля мають безпечні межі; чи не лишається `NaN` при 0/порожніх значеннях.

### 2. Логічний аудит фізики (`physics-model.js`)
- Перевір, що `draftPa` монотонно зростає з висотою димоходу і падає з кількістю вигинів.
- Перевір гейт: `secondaryActive=false`, якщо `combustionTempC<600` (без каталізатора); з каталізатором — знижений поріг.
- Перевір, що `Φ` попадає в **0.4–0.5** для режиму `medium` на Standard і що `MIX_RICH/MIX_LEAN` не спрацьовують на типових пресетах.
- Перевір, що `efficiencyPct` і `heatOutputKw` **фінітні** і в межах `[35,88]` та `[1,20]`.
- Перевір монотонність: більше ізоляції → вища `thermalRetentionPct`; більше вологості → нижча `combustionTempC`.

### 3. Числовий свип (напиши свій скрипт)
- Прогнати ≥200 комбінацій `width/depth/height/steel/firebrick` у межах UI.
- Для кожної: `designInternals` → `validateConfig` (valid?), `PhysicsModel.evaluate` (finite?), `buildBOM` (немає NaN, маса>0), `buildDXF`/`buildDrawingSVG` (валідні).
- Повідом кількість збоїв і перші приклади.

### 4. Тести
- Запусти `node tests/physics.test.js`. Очікування: `ALL TESTS PASSED`.
- Перевір, чи тести реально assert-ять (а не лише логують), і чи не «підігнані» під поточні числа.

### 5. UI-сценарії (live)
- Зміна габаритів → підказка «авто» оновлюється, 3D не ламається.
- «Призначення печі» → сауна/кімната/майстерня + обʼєм → «Підібрати» → фактична потужність наближається до потрібної (±20 %).
- Димохід: зменш висоту до 2 м + 3 вигини → тяга падає, зʼявляється `DRAFT_WEAK`; збільш → зникає.
- tertiary ON / catalyst ON → зʼявляються в 3D і в BOM; `SECONDARY_INACTIVE`/`CATALYST_COLD` працюють.
- Експорти: BOM CSV (кожен рядок рівно 11 колонок), DXF (лише сталь), SVG (підлога внизу, ніжки знизу, труба вгорі), GLTF/STL (без стрілок/зон/диму), Print/PDF (не порожня вкладка), Share (посилання відтворює 1:1).
- EN: усі тексти й варнінги англійською; експорти двомовні.

### 6. Негативні тести
- Мінімум 30×30×40 з шамотом 8 см → конфіг **валідний** (шамот авто-обмежується).
- Труба Ø10 → `CHIMNEY_NARROW`; Ø25 → `CHIMNEY_LARGE`.
- Слабкий димохід + вологість 28 % → `CREOSOTE_RISK` або `WET_WOOD`.

**ФОРМАТ ЗВІТУ (обовʼязково):**
1. Вердикт: `PASS / PASS WITH ISSUES / FAIL`.
2. Таблиця знайдених проблем: `# | Кроки | Очікувано | Фактично | Серйозність (Blocker/Major/Minor) | Місце в коді`.
3. Результати свипа: кількість кейсів, збоїв, приклади.
4. Результат `node tests/physics.test.js`.
5. 5 найсильніших сторін і 5 найризикованіших місць.
6. Оцінка **/10** з обґрунтуванням.
7. Гіпотези, які ти **не зміг** перевірити (напр., реальна тяга без CFD).

**КРИТЕРІЇ ПРОХОДЖЕННЯ:**
- 0 JS-помилок у консолі; тести проходять; 0 `NaN` у свипі; усі id/ключі знайдені; узгодженість фізики (не суперечить монотонності); експорти відкриваються.
- Будь-яка розбіжність між описом у звіті (`REPORT.md`) та фактичною поведінкою = **Major**.

**СЕРЕДОВИЩЕ:** вкажи браузер/ОС; якщо headless — уточни, що швидкість 3D не оцінювалась.

**ДЖЕРЕЛО ПРАВДИ:** цей репозиторій і `REPORT.md`. Усе, що суперечить фактичній поведінці — фіксуй як проблему.
