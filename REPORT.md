# Woodstove 2 — Звіт по проєкту

## 1. Загальна інформація

| | |
|---|---|
| Назва | **Woodstove 2** — 3D-конфігуратор дров'яної печі |
| Live | https://olehcheff.github.io/woodstove2/ |
| Репозиторій | `OlehCheff/woodstove2`, гілка `main` |
| Стек | чистий ES-модулі, three.js 0.160 (importmap), без збірки |
| Деплой | GitHub Pages (потрібен `Ctrl+F5` — конфіг кешується в localStorage) |

## 2. Структура

```
woodstove2/
├── index.html
├── css/styles.css
├── js/
│   ├── config.js         # конфіг, normalize, validate, encode/decode, пресети
│   ├── physics-model.js  # Physics v5 + optimizeConfig
│   ├── autodesign.js     # авто-проєктування внутрішньої геометрії
│   ├── room.js           # підбір печі під приміщення
│   ├── stove-builder.js  # 3D-геометрія (чиста функція)
│   ├── bom.js            # BOM, CSV, техкреслення SVG, DXF
│   ├── calibration.js    # калібрування за журналом Test Burn
│   ├── exporters.js      # GLTF/STL
│   ├── i18n.js           # UA/EN словники
│   └── app.js            # сцена, UI, керування
└── tests/physics.test.js # node-тести
```

## 3. Реалізований функціонал

**Геометрія / виріб**
- корпус зі сталі заданої товщини, передня панель з отвором під дверцята;
- дверцята: рама, скло, кріплення ручки, пружинна ручка-спіраль, засувка, петлі (ліва/права);
- футерування: шамот (світлий) — боки, зад, дно + передній бортик, з прошарком вермикуліт/CFB ізоляції між сталлю та шамотом;
- глухе дно (без колосника й зольника);
- primary — 2 овальні отвори в передній плиті під дверцятами + ковзна заслінка;
- secondary — **нижній вхід спереду** (щілина з повзуном і зеленою ручкою,
  привід `operation.secondaryAirPct`) → профільний канал під днищем **між
  ніжками** (поздовжній профіль по осі + поперечний колектор уздовж задньої
  кромки) → два вікна в 5-мм днищі → 2 задні підігрівальні стояки →
  поперечна SS-труба з отворами Ø3–5 мм. Кількість і Ø отворів рахуються від
  обсягу топки (0.082 см²/л): на Standard це 45×Ø5 мм ≈ 8.8 см² замість
  колишніх 15×Ø3 мм ≈ 1.06 см²;
- air-wash — кожух + флоп-заслінка + суцільна щілина над склом + бокова ручка;
- бафль — 2 уголки + знімний дефлектор + передній дефлектор;
- tertiary (опція) — третинна труба з дрібними отворами;
- каталізатор (опція) — стільник перед димоходом;
- газові канали, flue bell, люк чистки, димохід + комір;
- вихід труби: **верхній** (комір у кришці + flue bell) або **задній**
  (комір у задній панелі, горизонтальний патрубок, трійник 90° із заглушкою
  ревізії, вертикаль за піччю; кришка стає суцільною варильною поверхнею);
- маршрут димоходу: **над піччю** (вертикаль крізь стелю) або **у стінний
  димохід позаду** (верхньому виходу тоді потрібні підйом + коліно +
  горизонталь назад); довжина патрубка 15–150 см;
- вигляд: розріз, теплові зони, потоки, аеродинаміка, дим, explode.

**Автоматика**
- будь-яка зміна габаритів → автопідбір внутрішньої геометрії під макс. ККД;
- призначення печі: сауна / кімната / майстерня + обʼєм або площа×стеля → підбір габаритів;
- калібрування за журналом Test Burn (з демпфуванням).

**Експорт / дані**
- BOM CSV (11 колонок, двомовний, з лапками), DXF (лише сталь), техкреслення SVG, GLTF/STL;
- журнал тестів + CSV, share-link, compare, screenshot, Print/PDF.

**Мови:** UA/EN повністю.

## 4. Physics v5 — модель

**Тяга (повний димохід, плавучисть):**
```
Δp = g·H·(ρ_air − ρ_gas(stackGasC)) · bendPenalty · diaFactor − bafflePenalty
stackGasC = roomPipeM > 0 ? 20 + (stackTemp − 20)·exp(−0.3·roomPipeM) : stackTemp
ρ_gas = 1.2·293/(stackGasC+273)
bendPenalty = 1/(1+0.12·routeBends);  routeBends = bends + outletTurns
diaFactor = (D/15)^0.3
draftPa ∈ [3, 40]
```

**Вихід труби й маршрут (власні повороти, яких немає у слайдері «Вигинів»):**

| маршрут \ вихід | top | rear |
|---|---|---|
| `up` (над піччю)   | 0 | 1 |
| `wall` (стінний димохід позаду) | 2 | 1 |

```
connectorM  = clamp(connectorLengthCm, 15, 150)/100
roomPipeM   = route === 'wall' ? connectorM + (top ? d·0.3/100 + 0.4 : 0)
                               : (rear ? connectorM : 0)
roomPipeGainPct = sensibleFlueLoss·excessAirLossFactor·(1 − exp(−0.3·roomPipeM))
```
`roomPipeGainPct` — окрема метрика: тепло ДОДАТКОВОЇ одностінної труби в
кімнаті. В `efficiencyPct` і `heatOutputKw` воно НЕ входить — це властивість
монтажу, а не печі. База відліку — верхній вихід із маршрутом «над піччю»
(`roomPipeM = 0`); його власна вертикальна труба врахована в загальному
охолодженні `exp(−0.12·H)`, а не тут.

ККД ПЕЧІ від місця виходу не залежить: його задає внутрішній шлях газів
(бафль, димова полиця, задня перепускна стінка), а `gasPathCm` уже рахує
повну глибину. У свипі різниця `rear − top` — 0.0 п.п. (медіана), |Δ| < 0.5.

**ЗАСТЕРЕЖЕННЯ про 0.12 за поворот.** Це модельна умовність, відкалібрована
разом із рештою формули тяги, а не виміряний опір. За балансом мас швидкість
газів у трубі Ø17.5 на сталому режимі — 0.46–0.67 м/с, тож реальний трійник
коштує ζ·ρv²/2 ≈ 0.03–0.16 Па, а не ~2 Па. Чесний розклад дефіциту тяги
заднього виходу на `defaultConfig` (bends 0, H 5 м, Ø17.5, L 30 см):
30.5 Па → 29.1 Па (−4.6%) від ОХОЛОДЖЕННЯ патрубка, і далі до 25.9 Па
(−15% разом) — від умовності коліна. Температура на виході: 314 → 283 °C
(−31 °C). Коефіцієнт `0.3/м` — теж оцінка (діапазон 0.2–0.4), яку варто
відкалібрувати Test Burn-ом. Для пресета `MODEL_PRESETS.standard` (bends 1)
відповідні числа верхнього виходу — 27.1 Па і 309 °C.

**Температури:**
```
moistureTempPenalty = max(0, moisture% − 12) · 6                              [°C]
combustionTempC = 480 + flame·260 + secondaryPreheat·0.55 + retention·160 + residence·12 − moistureTempPenalty   [450,1100]
modeledFlueTempC = combustionTempC − passes·110 − ins·18 − refractory·12      [120,650]
exitFlueTempC = ambient + (modeledFlueTempC − ambient)·exp(−0.12·H)·exp(−0.3·roomPipeM) − routeBends·6   [40,600]  (ambient = 20°C;
                                                                                 експоненційне охолодження вздовж
                                                                                 труби, а не лінійне)
```

**Вторинне горіння (гейт):**
```
secondaryIgnitionC = catalyst ? min(600, lightoff) : 600
secondaryActive = combustionTempC ≥ secondaryIgnitionC
```

**ККД:**
```
moistureEffPenalty = max(0, moisture% − 12) · 0.15
moistureFlueLossPenalty = max(0, moisture% − 12) · 0.25   (прихована теплота пароутворення, що йде з димом)
combustionEff = 68 + (T−600)·0.04 + secondaryQuality·1.2 + retention·5 + residence
              + airMix·5 + staging + draftBonus·0.3 + targetFit − moistureEffPenalty
              + secondaryGate(+3 / −8) + effBias                              [48,92]
flueLoss = 7 + (flueTemp−150)·0.025 + (1−retention)·8 − passes·1.5 + moistureFlueLossPenalty   [8,28]
efficiency = combustionEff − flueLoss + catalystBonus(0 / +2.5)               [35,88]
```
Каталізатор рахується ПІСЛЯ стелі `combustionEff` (не всередині неї) — інакше
на печі, що й так впирається у стелю 92, бонус каталізатора був непомітний.

**Надлишок повітря:**
```
λ = clamp(1.4 + airMix·1.6, 1.1, 4);  Φ = 1/λ  (ціль 0.4–0.5)
```

**Потужність / закладка / час:**
```
gross = fireboxLiters·0.105·(0.35+0.65·airMix)·powerFactor·draftFactor·geometryFactor  saneKw [0,500] — лише запобіжник від NaN/∞, не робочий діапазон
heat  = gross·efficiency/100 · calibrationFactor                                       saneKw [0,500]; час горіння досі clamp [2,14] год — на high великих печей і в overnight показаний час упирається саме в цю межу
maxLoad = liters·0.12·0.9;  recommended = maxLoad·fillFactor
burn = usefulEnergy/heat · burnFactor · (1+steelMm·0.015)                              [2,14]
```

**Варнінги:**
`SMOKE_RISK, OVERHEAT_RISK, STEEL_OVERHEAT, WET_WOOD, INEFFICIENT_MODE, DIRTY_GLASS, DRAFT_WEAK, SECONDARY_RESTRICTED, SECONDARY_COLD, SECONDARY_INACTIVE, SECONDARY_INTAKE_LOW, AIRWASH_JETS, AIRWASH_LOW, BAFFLE_GAP, STARTUP_LONG, CHIMNEY_NARROW, CHIMNEY_LARGE, CREOSOTE_RISK, MIX_RICH, MIX_LEAN, CATALYST_COLD, CONNECTOR_LONG`.
`SECONDARY_INTAKE_LOW` — повзун нижнього входу нижче механічного упору 20 %.
Помилки/попередження валідатора виходу: `REAR_OUTLET_NO_ROOM` (комір не влазить
між бафлем і кришкою), `CONNECTOR_LONG` (патрубок > 40 см — норма печі за
СНиП 41-01-2003 / ДБН В.2.5-67; NFPA 211 додає ухил ≥2% і відступ 457 мм),
`REAR_SHIELD_CUT` (задній екран обрізано під комір — потрібен відступ до стіни),
`BOTTOM_INTAKE_NO_ROOM` (ніжки нижчі за ~4.5 см — під днищем немає місця для
каналу; піч лишається валідною, просто без повзуна),
`BOTTOM_INTAKE_CHOKED` (вузьке місце каналу менше за площу отворів — витрату
задає канал, а не повзун).

**Нижній вхід secondary (метрики, фізично нейтральні):**
```
holes  = π(Ø/2)²·n                       ← 0.082 см²/л топки (sizeSecondaryHoles)
slot   = 1.5·holes                       ← сталий множник, без порогу ширини
open   = slot · secondaryAirPct/100      ← повзун
path   = 1/√(1/holes_eff² + 1/open²)     ← послідовні опори, ДОВІДКОВО
```
`path` у λ не входить: λ рахується від відсотків повзунів (`airMix`), а не від
площ. Підігрів повітря в каналі (оцінка +20…90 °C) НЕ моделюється — нових
коефіцієнтів для цього не вводили (див. §7).

## 5. BOM (поточні значення, Standard за замовчуванням у UI)

- метал (сталь + нержавійка secondary-труби) ≈201 кг, шамот ≈79 кг, ізоляція ≈26 кг, скло ≈2,3 кг;
- різ (з розкладкою) ≈6,2 м², зварні шви ≈29,4 м, покупних 6, деталей 57;
- нижній вхід secondary додав 4 позиції і ≈3,5 кг (канал 73×40×2 + колектор
  30×40×2 + плита з щілиною + повзун) та ≈4,0 м шва (+16 %). Для порівняння:
  відкинутий варіант із суцільним лотком під усією піччю коштував би ≈+7,6 кг
  листа і ≈+5,1 м шва — і ніжку на 2-мм листі;
- ніжки виправлено: рахуються як профільна труба 50×50×3 (≈4,4 кг/м, 0,71 кг
  кожна), а не як суцільний пруток 19,6 кг/м (було 2,94 кг кожна). Через це
  загальна маса металу впала на ≈8,9 кг — це виправлення старої помилки BOM,
  а не економія від каналу;
- числа взято напряму з `buildBOM(designInternals(normalizeConfig(defaultConfig)))` —
  тобто те, що фактично показує UI на стандартній печі, а не окремий пресет
  без автопроєктування;
- усе — **оцінка**, не виробничий розрахунок.

## 6. Тести

`node tests/physics.test.js` → **ALL TESTS PASSED** (фізика, пресети, валідність, BOM, DXF/SVG, автопроєкт, калібрування, room).
Числовий свип: **256 комбінацій** габаритів → 0 збоїв.
Блоки 47–53 — нижній вхід secondary: **450 конфігурацій** (ширина × глибина ×
ніжки × сталь) без жодної колізії каналу з ніжками чи вікнами в днищі;
масштабування отворів від топки; SECONDARY_RESTRICTED спрацьовує лише з
вимкненим air-wash і при отворах ≲10 % від правила сайзингу (фізика бере
0.006 см²/л зі стелею 2 см², сайзинг — 0.082 см²/л; ці дві норми ще не
узгоджені, а правило 0.082 см²/л діє лише до ≈42.8 см² — 2 ряди × Ø5 мм); BOM/CSV/креслення з каналом; i18n-паритет, зокрема
перевірка, що кожен `data-i18n` з `index.html` є в обох словниках.

## 7. Відомі обмеження

**Нижній вхід secondary — де модель претендує на більше, ніж дає залізо:**

- **повзун і λ.** У моделі `airMix` бере ВІДСОТОК повзуна з вагою 0.35, тож
  слайдер Secondary 20→100 % рухає λ 2.01→2.46 і ККД на ~2.9 п.п. Фізично
  отвори secondary — ~9 % площі всіх входів повітря з урахуванням внутрішньої заслінки (~19 % при повністю відкритій) (було 2.7 %, і тоді
  твердження було зовсім неправдою). Тобто вплив слайдера на λ — це й далі
  ПРИПУЩЕННЯ моделі, просто вже не фантастичне. Чесний спосіб це прибрати —
  рахувати λ від площ, а не від відсотків; це окрема зміна фізики, яку тут
  свідомо не робили. Те саме написано в UI під Φ-тюнером;
- **відсоток повзуна ≠ частка витрати.** Щілина й отвори стоять послідовно
  (опір ∝ 1/A²), тож 20 % ходу — це ПОЛОЖЕННЯ, а не 20 % повітря. Метрика
  «Secondary: шлях (послідовно)» показує реальну послідовну площу;
- **два дроселі на одному шляху.** Площу отворів у фізиці масштабує ще й
  `baffle.airflowPct` (внутрішня пластина `baffleReg` без ручки на фасаді,
  значення підбирає оптимізатор: 45/55/65). Тепер на шляху secondary їх два —
  повзун на фасаді (`operation.secondaryAirPct`) і внутрішня заслінка; вони не
  узгоджені між собою. Питання власнику: лишати `baffleReg` як
  конструктивний параметр автопроєктування чи вивести його на фасад;
- **підігрів у каналі не моделюється.** Повітря в каналі під днищем і в
  стояках реально підігрівається (оцінка +20…90 °C), але свого коефіцієнта
  для цього немає, а на стелі `combustionEff = 92` він лише підняв би втрати
  в трубі. Нових коефіцієнтів навмисно не вводили;
- **зворотна тяга.** Рушійний тиск нижнього входу = −Δp_топки(висота труби
  secondary) + (ρ_пов − ρ_каналу)·g·Δz. Він додатний тільки тоді, коли повітря
  в каналі й стояках ГАРЯЧЕ. На холодному каналі (20 °C) шлях secondary
  перевертається РАНІШЕ за primary: на печі 400 °C з тягою +5 Па це −1.74 Па
  проти −0.53 Па у primary. Тобто «найнижчий отвір перевертається останнім» —
  неправда, і модель цього стаціонарно не показує взагалі;
- **теплове розширення й герметичність.** Канал приварений до гарячого 5-мм
  днища; шви з перервами пропускатимуть повітря повз повзун і повз упор 20 %.
  Пил з підлоги й сажа після зворотної тяги можуть забити отвори Ø3–5 мм —
  потрібні знімна кришка/чистка (питання власнику, деталі ще немає);
- **ніжки** лишаються привареними до 5-мм днища, канал проходить МІЖ ними
  (тест 47 перевіряє відсутність колізій на 450 конфігураціях). Варіант
  «суцільний лоток» відкинуто: ніжка на 2-мм листі дає ~170–290 МПа згину під
  ~780 Н, тобто межу текучості S235 ще до нагріву й динаміки;
- усі числа — інженерна оцінка, не CFD і не сертифікація;
- маса чутлива до товщини сталі (4–5 мм = важка піч);
- автодизайн обмежує шамот на малих печах;
- тяга рахується від «ідеального» димоходу без урахування вітру/температури зовні;
- задній вихід фізично неможливий на низьких печах: комір не влазить між
  бафлем і кришкою на корпусі 40 см завжди, на 50 см — приблизно в половині
  випадків. Тоді геометрія лишається валідною (оптимізатор повертає
  найкращого кандидата без фільтра), а UI показує `REAR_OUTLET_NO_ROOM`;
- коефіцієнт охолодження одностінного патрубка `0.3/м` — оцінка (0.2–0.4),
  а штраф 0.12 за поворот — модельна умовність (див. §4). Обидва варто
  відкалібрувати Test Burn-ом: виміряти T газу до й після патрубка;
- маршрут димоходу — стаціонарна модель: холодна горизонталь на РОЗПАЛІ
  (коли плавучості ще немає) окремо не моделюється;
- `CATALYST_COLD` описує перехідний стан прогріву каталізатора одразу після
  розпалу, а модель — стаціонарна (без часової осі): мінімальна досяжна
  температура зони допалювання в межах UI ≈590°C, тож поріг займання
  каталізатора (150–500°C у налаштуваннях) фактично завжди нижчий за неї.
  Це обмеження моделі, а не помилка порогу.
- ще 10 кодів (`SMOKE_RISK`, `STEEL_OVERHEAT`, `INEFFICIENT_MODE`,
  `DIRTY_GLASS`, `SECONDARY_RESTRICTED`, `AIRWASH_JETS`, `AIRWASH_LOW`,
  `BAFFLE_GAP`, `MIX_RICH`, `MIX_LEAN`) жодного разу не спрацювали на
  1350-конфігураційному свипі: усі залежать від `primaryAir.openPct`,
  `operation.secondaryAirPct`, `airWash.gapCm/intakePct`,
  `baffle.airflowPct/frontGapCm` — параметрів, які повністю визначає пресет
  режиму й оптимізатор бафля, без окремого UI-контролю (в інтерфейсі немає
  Expert-панелі, хоча в `i18n.js` лишились переклади для неї — див. нижче).
  Це не помилка порогу в кожному з 10 кодів окремо, а один спільний
  архітектурний наслідок: попередження лишаються в моделі для сценаріїв
  ручного/API-редагування конфігурації, а не для звичайного UI-потоку.
  `CHIMNEY_NARROW`/`CHIMNEY_LARGE` теж не спрацьовують у тому самому свипі —
  але це навмисний результат (Major #8): автопідбір діаметра тепер завжди
  влучає у власний коридор валідатора; попередження лишається як захист від
  зовнішнього/вручну відредагованого конфігу з невідповідним діаметром.
- **модель стаціонарна.** PhysicsModel рахує лише усталений режим горіння,
  без часової осі: розпал і прогрів, дозавантаження, догоряння вугілля не
  моделюються — у цих фазах реальні ККД/потужність/температури/димність
  інші. Режим «start-up» у Test Burn — той самий стаціонарний розрахунок з
  іншими заслінками, НЕ модель прогріву. Дисклеймер тепер явний в UI (біля
  KPI Фізики v5, у Test Burn над чекбоксом «Не калібрувати start-up», у BOM,
  у друкованому звіті, у штампі креслення SVG і в CSV) — див. §8.

## 8. Виробничий шар (block G)

Новий модуль `js/production.js` (чисті функції, без DOM/three) і три
функції в `js/bom.js` — `bomGeometry`, `buildWeldPlan`, `buildFitPlan` —
переводять BOM з «розмірів 1:1 з 3D» у виробничі дані: допуски, теплові
зазори вільних деталей і план зварних швів ISO 2553.

**Джерела формул:**
- теплова деформація вуглецевої сталі — EN 1993-1-2, 3.4.1.1 (20…1200 °C);
  аустенітна нержавійка — той самий стандарт, додаток C; шамот — оцінка
  (α ≈ 5.5e-6/K, середнє 20…1000 °C);
- допуски деталей — ISO 2768-1, клас m; допуски зварних вузлів (габарити
  корпусу) — ISO 13920, клас B;
- катет кутового шва `a ≈ 0.7·t`, мінімум 3 мм — EN 1993-1-8, 4.5.2;
- символи зварних швів на кресленні — ISO 2553, система A (компактна
  виноска на виді + повний символ у таблиці швів);
- різ листа/труби — ISO 9013; якість шва — ISO 5817, клас C; процес — 135
  (MAG).

**Розрахункові температури** фіксовані, без прив'язки до фізики (бафль і
нержавійка 800 °C, шамот 700 °C, корпус 20 °C — найгірший перехідний
випадок: гаряча деталь у щойно розпаленому холодному корпусі). Для
default-печі це дає бафль 690 мм → +7.6 мм → зазор 5 мм/бік, різ 680 мм;
трубі secondary — осьовий хід ~5–9 мм залежно від пресету.

**План швів** — 12 фіксованих груп (W1–W12: оболонка, стики смуг, комір,
внутрішня труба, поздовжній шов труби, ніжки, рама дверцят, петлі, уголки
бафля, газоходи, повітряні канали, люк чистки). Раніше `weldCm` кожної
панелі в BOM дорівнював її повному периметру — кожне спільне ребро
рахувалось двічі (типове завищення ~2×); тепер `weldCm` і `weldRef` кожної
деталі беруться з `weldPlan.byPart`, а офіційний підсумок
`totals.weldMeters` — це сума 12 груп (`weldPlan.totalM`), не сума
per-деталь оцінок.

**Два дефекти геометрії, які дослідження показало відкритими, тепер
закриті в самому коді (а не замасковані на кресленні):**
- уголок опори бафля — раніше смужка завтовшки з лист (полиця 5 мм),
  тепер рівнобокий профіль **L25×25×3** (L30×30×3 на найбільших печах,
  `config.js:baffleAngleSizeCm`), однаковий у BOM і в 3D
  (`stove-builder.js`). Опора з протилежного боку (`F2`, найгірший випадок
  — бафль зсунутий до однієї стінки) тепер завжди ≥10 мм;
- перекриття дверцят/отвору — уже виправлено до цього блоку (15 мм/бік,
  `config.js:DOOR_OVERLAP_CM`, `doorOpening()`); `F9` на кресленні лише
  підтверджує число.

**Відкриті інженерні рішення (не закриті в цьому блоці, свідомо):**
- діаметр труби з автопроєктування лишається нестандартним (напр. 17.5,
  20.5 см) — округлення до ряду покупних труб не зроблено: зачіпає
  коридор валідатора й ідемпотентність (тести 22/30), рішення власника;
- бафль фіксується на **передньому упорі** (гарячий передній прохід =
  той самий, що в моделі; холодна задня щілина працює як байпас на
  розпалі) — альтернатива «задній упор» звужувала б передній прохід газів
  на 14–32 % на найбільших печах;
- поточний розподіл панелей корпусу (смуги фасаду/верху) лишено як є.

**Креслення SVG** (`buildDrawingSVG`) отримало компактні виноски ISO 2553
на видах (ліва колонка біля фронтального виду — по одній на групу),
повну таблицю швів і таблицю посадок праворуч (рядок з `status:'fix'`
підсвічується `#b42318` — на поточній геометрії жодного немає), блок
технічних вимог (ISO 2768-mK / ISO 13920-BF / ISO 2553 / ISO 9013 /
ISO 5817-C) і штамп із дисклеймером стаціонарної моделі. Розмірні лінії
корпусу (W/H/H загальна/ніжки/D) тепер несуть допуск ISO 13920-B.
CSV лишається рівно на 11 колонках — додано два рядки, `ДОПУСКИ`/
`TOLERANCES` і `ПРИМІТКА`/`NOTE`, з тим самим числом полів, що й решта
таблиці.

Тести 61–66 (`tests/physics.test.js`) покривають: монотонність і межі
теплової деформації; посадки на всіх пресетах + мінімальному (30×30×40,
t3) і найбільшому (140×120×180, t8) корпусі; узгодженість виробничих
розмірів бафля в BOM з планом посадок (≤1 мм); стабільність ID плану швів
(у т.ч. коли зникає група `legs`); валідність SVG (баланс тегів, без
сирого `&`, кожен `W#` — щонайменше двічі, `±` на розмірах, дисклеймер у
штампі, без кирилиці в EN); CSV на 11 колонок з новими рядками; паритет
ключів `STR`/`PROD_TXT` uk/en і відсутність кирилиці в EN-текстах.
